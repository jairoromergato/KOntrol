import SwiftUI

enum AppColors {

    // Fondos (sin blanco)
    static let background = Color(hex: "#E6F1ED")      // verde salvia muy claro
    static let cardBackground = Color(hex: "#F0F7F5")  // verde agua claro

    // Texto
    static let primaryText = Color(hex: "#243A3A")     // azul petróleo suave
    static let secondaryText = Color(hex: "#5F7C7C")   // azul grisáceo

    // Botones
    static let primaryButton = Color(hex: "#6FAF9B")   // verde eucalipto
    static let secondaryButton = Color(hex: "#7FAEC7") // azul pastel

    // Bordes / separadores
    static let divider = Color(hex: "#C7DDDA")
}
